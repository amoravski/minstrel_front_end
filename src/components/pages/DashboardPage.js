import React from 'react';
import Sidebar from '../Sidebar';

const Dashboard = () => {
    return (
        <div>
            Dashboard
            <Sidebar dashboard="active"/>
        </div>
    );
}

export default Dashboard;
